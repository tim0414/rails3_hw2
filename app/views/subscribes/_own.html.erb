<tr>
    <td class="subscribe">
        <b>Name:</b>
        <span class="subscribe"><%= User.find(own.owner_id).name %></span>
        <b> Email:</b>
        <span class="subscribe"><%= User.find(own.owner_id).email %></span>
    </td>
</tr>