<tr>
    <td class="subscribe">
        <b>Nickname:</b>
        <span class="subscribe"><%= subscribe.nickname %></span>
        <b> Email:</b>
        <span class="subscribe"><%= User.find(subscribe.subscriber_id).email %></span>
    </td>
</tr>